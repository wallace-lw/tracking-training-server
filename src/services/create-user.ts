// import { prisma } from '../../prisma/connect'

// import { User } from '../interfaces'

// export class CreateUserService {
//   async perform({ name, username, password }: User) {
//     await prisma.user.create({ name, username, password })
//   }
// }
